import React from 'react'
import  ReactDOM  from 'react-dom'

//inserting data in main div of index.html
//first argument is the data that needs to be rendered
//second argument is place where first argument needs to be inserted, main div
ReactDOM.render(<h1>My App</h1>, document.getElementById('root'))